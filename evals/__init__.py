"""Eval tools — require Claude CLI. Not part of the core linter.

These are development tools for paper authors who want LLM-scored
assessments of their citation quality. They require a Claude API
subscription and the `claude` CLI installed.

The core sciwrite-lint package uses only local vLLM — no cloud services.
"""
